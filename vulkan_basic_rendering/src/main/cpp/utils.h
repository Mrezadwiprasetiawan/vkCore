#pragma once
#include <vulkan/vulkan.h>
#include <jni.h>
#include <sstream>
#include <string>
#include <vector>

std::string InsVers(void);
std::string API(void);
std::string versionToString(uint32_t versionCode);